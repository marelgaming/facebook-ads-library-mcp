FROM python:3.12-slim

WORKDIR /app

COPY requirements-google-remote.txt .
RUN pip install --no-cache-dir -r requirements-google-remote.txt

COPY google_remote_server.py .

ENV PORT=8000
EXPOSE 8000

CMD ["python", "google_remote_server.py"]
